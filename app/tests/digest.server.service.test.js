'use strict';

/**
 * Module dependencies.
 */
var should = require('should'),
  app,
  config = require('../../config/config');

/**
 * Globals
 */


/**
 * Unit tests
 */

describe('Digest Unit Tests:', function() {

  describe('Method Find', function() {

    it('should be able to return query with correct conditions', function(done) {

      //var query = DigestService.getQuery();
      //should.exist(query._conditions.created.$gt);
      //should.exist(query._mongooseOptions.populate.user.match.last_digest_time.$lt);
      done();
    });

  });

});